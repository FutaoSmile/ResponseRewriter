# Product

## Register

product

## Users

需要调试和改写网页接口响应的开发人员及其他技术用户。

## Product Purpose

通过可配置、可排序的规则匹配 XHR 与 Fetch 请求，替换、合并或动态转换响应，也可以拦截 Fetch 并在不访问服务器的情况下直接返回内容，帮助用户安全、可控地完成接口调试和场景模拟。XHR 命中仅支持 Fetch 的规则时会明确标记为已放行。

## Brand Personality

专业、简洁、高效。

## Anti-references

避免营销化表达、花哨装饰和妨碍任务操作的复杂交互。

## Design Principles

- 在配置位置直接解释技术概念并提供可复制的示例。
- 首次安装提供停用的完整模式示例和明确标记的演示日志，让用户无需先配置即可理解规则与结果。
- 首次启用拦截前显著说明请求 URL 与响应日志的本地处理方式，并在用户明确同意前保持拦截器未注入。
- 在拦截详情中突出真正发生变化的响应内容。
- 保持操作路径短，优先展示完成任务所需的信息。
- 使用一致、明确的术语描述规则和处理结果。
- 让规则执行顺序、全局启停状态和数据保留限制始终可见。
- 规则排序使用直接拖拽的标准手柄，并为键盘用户提供等价操作。
- 运行失败必须留下可理解、可追踪的结果，不以静默回退代替反馈。
- 保留无需构建即可加载扩展的简单工作流。

## Accessibility & Inclusion

保持清晰的文本对比度、键盘可操作性，并尊重系统的减少动画设置。

# UI / UX 整改跟踪

本文件用于跟踪 2026-07-27 产品、UI 与 UX 全量审计后的整改工作。状态以当前代码和验收证据为准，不以实现意图为准。

## 验收范围

- 支持中文、英文、日文、韩文。
- 支持浅色与暗色主题。
- 覆盖 375、768、1024、1440px 布局。
- 覆盖鼠标、纯键盘和读屏基本操作。
- 覆盖空数据、筛选无结果、字段错误、保存失败、运行失败、日志截断和日志达到上限。
- 不启动或编译项目；使用源码检查和现有 Node 测试完成可执行范围内的验证。

## P1

- [x] P1-01 规则执行顺序可见且可调整
  - 列表说明规则从上到下执行。
  - 使用规则左侧拖拽手柄排序，操作列不再展示上移、下移箭头。
  - 手柄支持 `Alt+↑/↓` 键盘排序，当前规则保持整行高亮并播报移动后的位置。
  - `mock-fetch` 明确说明首条命中生效。
  - 验收：排序写入存储，普通规则管线与 `mock-fetch` 优先级均跟随列表顺序。

- [x] P1-02 规则列表具备规模化管理能力
  - 支持按名称、方法和 URL 搜索。
  - 支持按启用状态、响应处理方式筛选。
  - 筛选无结果时可一键清除筛选。
  - 验收：筛选与分页使用同一结果集，切换语言后状态不丢失。

- [x] P1-03 所有可点击内容具备正确语义
  - 命中次数、日志卡片、命中详情使用原生按钮或等价键盘语义。
  - 动态开关具有本地化可访问名称和可见焦点。
  - 验收：仅用 Tab、Enter、Space 可完成全部列表操作。

- [x] P1-04 弹窗焦点、层级与未保存保护完整
  - 所有弹窗具有 `dialog` / `alertdialog` 语义、标题关联和焦点锁定。
  - Escape 只关闭最上层弹窗。
  - 关闭后焦点返回触发控件。
  - 编辑内容发生变化时关闭需确认。
  - 验收：嵌套确认、取消、保存、Escape、背景点击均符合预期。

- [x] P1-05 窄屏规则列表保留字段语义
  - 1180px 以下使用带字段标签的规则卡片，确保 1024px 宽度下不挤压表格。
  - 操作区和状态区不依赖桌面表头。
  - 验收：375px 下无横向溢出、无无标签数据。

- [x] P1-06 表单错误和全局反馈可定位、可读屏
  - 字段错误就地显示并关联 `aria-describedby`。
  - 保存、错误等全局消息使用 `role=status` / `role=alert`。
  - Toast 停留时间适合长翻译，并允许主动关闭。
  - 验收：提交后聚焦第一个错误字段，读屏可收到结果。

- [x] P1-07 运行和存储失败不再静默
  - JavaScript 转换、JSON 合并失败产生明确失败日志。
  - 清空、删除、保存、偏好设置均处理存储错误。
  - 失败时不提前关闭弹窗或展示成功状态。
  - 验收：模拟失败时数据和界面保持一致，并提供可理解的错误消息。

- [x] P1-08 日志保留和截断限制透明
  - 日志区显示仅保留最近 100 条。
  - 被截断的响应显示标识、原始长度和当前限制。
  - README 中同步说明限制。
  - 验收：达到限制和超过 20000 字符均有可见提示。

- [x] P1-09 提供全局安全控制
  - 顶部提供全局暂停开关和持续可见状态。
  - 页面说明扩展运行在所有 URL / frame。
  - 验收：暂停后不再拦截。

## P2

- [x] P2-01 移除装饰性动效并支持减少动画
- [x] P2-02 建立 28 / 34 / 42px 桌面密度和窄屏 44px 触控规则
- [x] P2-03 操作列由内容驱动，不再按语言硬编码宽度
- [x] P2-04 工作区扩展至 1440px，日志区域保持合理上限并将新增空间优先分配给规则列表
- [x] P2-05 空状态提供直接行动入口和使用说明
- [x] P2-06 导入前提供数量、冲突与追加策略预览
- [x] P2-07 删除未使用且已失效的 `popup.html`
- [x] P2-08 清理内联样式，补齐搜索框、开关等焦点状态，并为图标操作提供即时悬浮提示
- [x] P2-09 将“清空日志”归入日志标题区域，并在窄屏保持横向紧凑，避免与清除筛选混淆
- [x] P2-10 约束规则列表单元格收缩与截断，避免规则名称侵入方法列
- [x] P2-11 将全局拦截开关设计为与顶部控件等高的紧凑状态栏，并通过即时帮助说明暂停影响
- [x] P2-12 首次安装初始化停用的全模式示例规则和不计入真实命中的示例日志
- [x] P2-13 拍平命中详情中的嵌套状态标签，减少重复框线

## 文档与验证

- [x] 同步 `README.md`、`README.en.md`、`PRODUCT.md` 和示例配置。
- [x] 增加规则顺序、全局暂停、运行失败和日志限制测试。
- [x] 增加四语言结构完整性、无障碍语义和响应式源码约束测试。
- [x] 运行全部 Node 测试：2026-07-28，63/63 通过。

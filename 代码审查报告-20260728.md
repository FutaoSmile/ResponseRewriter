# 变更审查报告

## 概览

- 扫描仓库：1 个；有变更仓库：1 个。
- 对比基准：`origin/master`；当前分支：`master`；`HEAD` 与基准均为 `b971070`。
- 提交数：0；工作区变更：7 个文件，新增 397 行、删除 193 行。
- 审查范围：未提交的界面与文档变更，并补充 Chrome 网上应用店上架所需的整包安全、隐私与合规检查。
- 整体风险：🔴 高风险。
- 结论：**当前版本不建议提交 Chrome 网上应用店审核。** 有 3 项发布阻断问题，需修复并复验后再上架。

## chrome-xhr-interceptor — 分支: master

### 变更摘要

本次工作区变更重新设计了主题、语言和全局拦截控件，压缩了规则列表说明，并统一收窄滚动条；中英文 README 同步更新了对应说明和锚点。核心拦截逻辑、Manifest 与权限未在本次工作区中修改，但上架前整包检查发现了既有的页面通信和隐私合规风险。

### 提交列表

- 当前分支相对 `origin/master` 无新增提交。
- 本报告审查的是 7 个未提交文件的工作区差异，以及上架前必须核对的现有 Manifest、内容脚本和页面脚本。

### 问题与风险

#### 1. 响应式 CSS 少一个闭合括号，窄屏布局会被错误解析

- **位置**：`src/ui/popup.css:2386`
- **问题描述**：`.header-divider {` 后缺少声明和 `}`，紧接着又开始 `.manager-rules .list-head` 选择器。当前文件共有 365 个 `{`、364 个 `}`。浏览器会把后续响应式规则错误地嵌套到 `.header-divider` 中，导致宽度不超过 1180px 时列表表头隐藏、单列卡片布局等规则不能按预期生效。现有测试只用正则检查样式文本存在，因此 63 项测试全部通过也没有发现该语法错误。
- **风险等级**：🔴 严重
- **修复建议**：恢复完整的响应式选择器和闭合结构，并增加 CSS 可解析性检查：

```css
.header-divider {
  display: none;
}

.list-head,
.manager-rules .list-head,
.log-head {
  display: none;
}
```

修复后至少重新核对花括号数量，并在 1180px、640px 两个断点实际检查规则列表和日志列表。

#### 2. 页面通信标记可被任意网页监听和伪造，规则内容存在泄露与篡改风险

- **位置**：`src/content.js:80-92`、`src/content.js:176-188`、`src/injected.js:431-447`、`src/injected.js:661-670`
- **问题描述**：隔离世界把完整规则数组通过 `window.postMessage(..., "*")` 发到页面世界，其中可包含内部 URL、模拟响应正文和用户脚本。普通网页脚本可以监听这些消息；`response-rewriter-extension` 与 `response-rewriter-page` 只是源码中公开的固定字符串，不是“私有标记”或认证机制。网页也可以伪造 `SET_RULES` 改写当前页拦截行为，或伪造 `RULE_HIT` 污染扩展日志和命中统计。该问题不是本次 UI 差异引入，但属于上架前必须处理的安全边界缺陷。
- **风险等级**：🔴 严重
- **修复建议**：不要再通过页面可观察的广播消息传输完整规则或把固定 `source` 字符串当作可信身份。优先使用 `chrome.scripting.executeScript` 的 `MAIN` world 与参数传递更新闭包内状态，或设计不向页面暴露规则正文的专用桥接方式；页面到扩展的日志通道必须具备不可由页面脚本伪造的边界。至少补充以下回归测试：
  1. 页面脚本不能读取其他规则的 URL、响应正文或脚本；
  2. 伪造 `SET_RULES` 不能替换活动规则；
  3. 伪造 `RULE_HIT` 不能写入 `chrome.storage.local`。

#### 3. 缺少隐私政策、显著披露和明确同意流程

- **位置**：`src/content.js:134-154`、`README.md:106-116`、`README.en.md:106-116`；仓库中不存在隐私政策文件。
- **问题描述**：扩展会处理请求 URL、原始响应正文和改写后正文，并将最近 100 条日志保存在 `chrome.storage.local`。Chrome 网上应用店明确规定，即使数据只保存在本地，只要扩展处理用户数据也必须提供隐私政策；处理前还需要在产品界面中显著说明数据类型和用途，并通过用户明确操作取得知情同意。当前 README 仅说明“本地保存规则和日志”，没有完整列出 URL、原始/改写响应、保留期限、共享情况和删除方式，也没有首次启用前的披露与同意流程。官方 2026 年 7 月 1 日更新进一步要求显著披露所有数据收集，并将于 2026 年 8 月 1 日开始执行。
- **风险等级**：🔴 严重
- **修复建议**：
  1. 新增准确、公开可访问的隐私政策，写明处理的数据、用途、本地存储、100 条/20,000 字符保留限制、不向开发者或第三方传输、删除方式和 Limited Use 承诺；
  2. 在首次启用拦截或首条规则生效前显示不可跳过的显著披露，并要求用户主动同意；
  3. 同步更新 README、产品内说明和商店详情页；
  4. 在开发者后台 Privacy 页如实声明网站内容、请求 URL/浏览活动等数据处理，并填写隐私政策链接与单一用途。

依据：[Chrome Web Store Program Policies](https://developer.chrome.com/docs/webstore/program-policies/policies)、[User Data FAQ](https://developer.chrome.com/docs/webstore/program-policies/user-data-faq)、[2026 policy update](https://developer.chrome.com/blog/cws-policy-updates-2026?hl=en)。

#### 4. `<all_urls>`、所有子 frame 与可访问资源范围较宽，需要更强的最小权限论证

- **位置**：`manifest.json:17-48`
- **问题描述**：扩展在所有 URL、所有子 frame 中注入内容脚本，并向所有 URL 暴露 `src/injected.js`。这与“对任意网站配置响应改写”的核心用途相关，不一定构成违规，但会提高人工审核和隐私审查强度。当前规则未启用时仍在所有页面安装 XHR/Fetch 包装器；商店审核人员可能要求说明为何不能只对用户授权的网站运行。
- **风险等级**：🟡 警告
- **修复建议**：评估改为 `optional_host_permissions` 并按用户选择的网站申请权限；若产品必须保留全站权限，应在单一用途、权限说明、商店详情和产品界面中一致说明必要性，并确保无活动规则时不处理或记录请求数据。Chrome 要求申请实现功能所需的最窄权限，参见 [Program Policies](https://developer.chrome.com/docs/webstore/program-policies/policies)。

#### 5. 用户脚本通过 `new Function` 执行，需避免被判定为远程代码

- **位置**：`src/injected.js:218-221`
- **问题描述**：JS 转换规则使用 `new Function` 执行用户填写或从本地 JSON 导入的脚本。当前检查未发现从网络下载脚本或远程配置，代码也有明确注释，因此不直接等同于远程托管代码；但这一模式是 MV3 审核重点，导入规则中的逻辑可能让审核人员质疑功能是否能从提交包中完整辨识。
- **风险等级**：🟡 警告
- **修复建议**：在审核说明中明确脚本只能由用户手动输入或选择本地文件导入，扩展不会下载、同步或接收远程代码；禁止未来从 URL 获取规则脚本。若审核反馈要求调整，迁移到 Chrome 明确允许的 `userScripts` API。依据：[Manifest V3 requirements](https://developer.chrome.com/docs/webstore/program-policies/mv3-requirements/)。

### 已通过的检查

- `node --test`：63 项通过，0 失败。
- 全部 JavaScript 文件执行 `node --check`：通过。
- `manifest.json` 可正常解析，Manifest V3、名称、版本与图标引用完整。
- 16、32、48、128 像素图标尺寸均正确。
- 未发现硬编码密钥、Token、私钥或远程脚本地址。
- `git diff --check` 未发现空白错误，仅提示 Windows 行尾转换。
- README 中“注意事项和限制”锚点已改为实际标题对应的 `#注意事项` / `#notes`。

## 发布建议

- **是否可以安全发布**：不可以。先修复问题 1、2、3，再进行一次完整复验。
- **建议顺序**：
  1. 修复 CSS 结构并增加可解析性测试；
  2. 重构页面通信安全边界并增加伪造/泄露测试；
  3. 完成隐私政策、首次披露与同意流程，随后同步 README 和商店资料；
  4. 重新评估全站权限，并准备 `new Function` 的审核说明；
  5. 最后重新运行测试、语法检查和真实 Chrome 手工验证，再生成干净的上架 ZIP。
- **商店后台人工核对**：开发者账号已启用两步验证；Store Listing 有完整描述、图标和截图；Privacy 页的单一用途、权限理由、数据类型和隐私政策互相一致。上传和隐私声明入口可参考 [Chrome Web Store 发布流程](https://developer.chrome.com/docs/webstore/publish)。
- **回滚注意事项**：本次工作区没有数据结构迁移。若 UI 改动需回滚，只回退这 7 个未提交文件，不要覆盖用户已有规则和 `chrome.storage.local` 日志。

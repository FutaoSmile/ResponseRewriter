# 变更审查报告

## 概览

- 扫描仓库：1 个；有变更仓库：1 个。
- 对比基准：`origin/master`；审查时当前分支为 `master`，`HEAD` 与基准均为 `b971070`。
- 提交数：0；待提交内容共 26 个文件路径，其中包含 2 个明确删除项和 4 个新增文件。
- 审查范围：Chrome 网上应用店上架前修复、界面与图标调整、相关测试和文档同步，以及目录与无效代码清理。
- 整体风险：🟡 中等风险。
- 结论：**可以提交本次代码。** 未发现代码层面的发布阻断问题；正式提交商店审核前仍需完成真实 Chrome 冒烟测试和商店后台人工核对。

## chrome-xhr-interceptor — 分支: master

### 变更摘要

- 修复响应式 CSS 结构，并完善主题、语言、全局拦截、规则说明、提示气泡和窄滚动条等界面细节。
- 将页面拦截器改为 Manifest 声明的 `MAIN` world 脚本，通过每个 frame 随机生成的事件通道同步规则，移除公开的 `window.postMessage` 规则广播。
- 增加首次启用前的隐私披露与明确同意；同意前不安装 XHR/Fetch 拦截器，并新增中英双语隐私政策。
- 更新扩展图标和 README 宣传图，保留固定的 `icon-master.png` 作为唯一母图。
- 删除已不再使用的 `scripts/gen-icons.js` 和旧 `docs/reviews` 审查快照目录；移除不存在的主题下拉框引用及尚无真实商店 ID 的禁用入口。
- 同步更新中英文 README、产品说明、Manifest、测试和测试数量。

### 提交列表

- 审查时当前分支相对 `origin/master` 无新增提交。
- 本报告审查的是即将提交的完整工作区差异；历史报告 `代码审查报告-20260728.md` 作为时间点快照保留，不回写其旧结论。

### 问题与风险

#### 1. JavaScript 转换仍使用 `new Function`，可能触发商店人工复核

- **位置**：`src/injected.js`
- **问题描述**：JavaScript 转换规则会执行用户在管理页填写或从本地文件导入的转换代码。当前实现没有下载远程代码，源码注释、README 和隐私政策也说明了数据边界，但 `new Function` 仍属于 Manifest V3 审核的敏感实现。
- **风险等级**：🟡 警告
- **建议**：在商店审核说明中明确代码只来自用户主动输入或本地导入，不会从网络获取、同步或远程下发；商店资料不要将此能力描述成可加载远程脚本。

#### 2. 全站及所有子 frame 权限需要在商店后台保持一致说明

- **位置**：`manifest.json`
- **问题描述**：扩展使用 `<all_urls>` 和 `all_frames`，这是对任意目标页面改写 XHR/Fetch 响应所需的产品能力，但权限范围较宽，审核时通常需要清晰解释。当前隐私政策和 README 已说明用途，同意前也不会安装拦截器。
- **风险等级**：🟡 警告
- **建议**：在 Privacy 与权限说明中使用与仓库一致的表述，并如实声明请求 URL、请求方法、原始响应和改写后响应的本地处理与保存方式。

#### 3. 真实浏览器与商店后台状态不在本次自动检查范围内

- **位置**：发布流程
- **问题描述**：按项目约定，本次没有编译或启动扩展，因此没有重新执行真实 Chrome 的安装、首次同意、跨 frame 拦截、主题/语言弹层和窄屏布局冒烟测试；开发者账号两步验证、商店隐私表单、截图和公开隐私政策链接也无法从仓库内确认。
- **风险等级**：🟡 警告
- **建议**：提交商店审核前在最低支持版本 Chrome 114 及当前稳定版各完成一次手工冒烟，并逐项核对商店后台声明。

### 已通过的检查

- `npm test`：69 项通过，0 失败。
- 全部 JavaScript 文件执行 `node --check`：通过。
- `manifest.json` 可正常解析；Manifest V3、最低 Chrome 版本、脚本顺序和图标引用完整。
- `src/ui/popup.css` 花括号数量一致。
- `git diff --check` 未发现空白错误，仅有 Git 的 Windows 行尾转换提示。
- 未发现残留的 `window.postMessage`、`eval`、调试输出、旧图标生成命令或已删除目录引用。
- README 中只引用固定母图 `src/icons/icon-master.png`，Manifest 使用 16、32、48、128 像素成品图标。
- 相关功能行为已同步到 `README.md`、`README.en.md`、`PRODUCT.md` 和 `PRIVACY.md`。

## 发布建议

- **是否可以提交代码**：可以。
- **是否可直接提交商店审核**：完成真实 Chrome 冒烟测试及商店后台人工核对后可以。
- **提交后建议核对**：
  1. 首次安装时默认暂停拦截，未同意隐私披露前页面原生 XHR/Fetch 不被替换；
  2. 同意后规则、日志、主题、语言和全局开关行为正常，主 frame 与子 frame 均符合预期；
  3. 商店详情、Privacy 表单、权限理由和公开的 `PRIVACY.md` 内容一致；
  4. 审核备注解释 `<all_urls>`、`all_frames` 与用户本地 JavaScript 转换的必要性和边界。
- **回滚注意事项**：隐私同意版本保存在 `chrome.storage.local`；回滚到旧版本不会删除用户规则和日志，但旧版本不会识别新的同意门控语义。
